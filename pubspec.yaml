name: github_metrics
description: Prometheus metrics for GitHub repos.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  args:
  http:
  tuple: 2.0.0-nullsafety.0
